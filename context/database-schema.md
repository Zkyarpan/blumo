# Blumo — Database Schema

## General Rules

- Use PostgreSQL UUID primary keys generated by the database.
- Use `timestamptz` for all timestamps.
- Store user-facing local timezone as an IANA timezone string.
- Add `created_at` and `updated_at` where records may change.
- Enable Row Level Security on every user-owned table.
- Use foreign keys with explicit delete behaviour.
- Never expose service-role access to the browser.
- Prefer database constraints over application-only assumptions.

## Tables

### `profiles`

Extends the Supabase Auth user.

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key; equals `auth.users.id` |
| `github_user_id` | bigint | GitHub's stable numeric user ID |
| `github_username` | text | Current GitHub login |
| `display_name` | text | Nullable |
| `avatar_url` | text | Nullable |
| `timezone` | text | Default `Europe/London` only when selected or detected with consent |
| `experience_level` | text | `beginner`, `intermediate`, or `advanced` |
| `onboarding_completed_at` | timestamptz | Nullable |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

Constraints:

- `github_user_id` unique when present.
- `github_username` stored in normalized form for lookups.
- `experience_level` checked against allowed values.

### `goals`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `title` | text | Human-readable goal |
| `technology` | text | Initial selected technology |
| `task_type` | text | Initial preferred task type |
| `daily_minutes` | integer | Allowed values: 10, 20, 30, 45, 60 |
| `status` | text | `active`, `paused`, `completed`, `archived` |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

MVP rule:

- A user may have at most one goal with `status = 'active'`.
- Enforce through a partial unique index.

### `github_installations`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `installation_id` | bigint | GitHub installation ID |
| `account_id` | bigint | GitHub user or organization ID |
| `account_login` | text | GitHub account login |
| `account_type` | text | `User` or `Organization` |
| `status` | text | `active`, `suspended`, `uninstalled` |
| `installed_at` | timestamptz | Nullable |
| `suspended_at` | timestamptz | Nullable |
| `uninstalled_at` | timestamptz | Nullable |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

Constraints:

- `installation_id` unique.
- Do not store installation access tokens.

### `repositories`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `installation_id` | uuid | References `github_installations.id` |
| `github_repository_id` | bigint | Stable GitHub repository ID |
| `owner` | text | Repository owner login |
| `name` | text | Repository name |
| `full_name` | text | `owner/name` |
| `default_branch` | text | Usually `main`, but never assumed |
| `is_private` | boolean | Repository visibility |
| `is_selected` | boolean | Active repository for the MVP |
| `access_status` | text | `active`, `removed`, `unavailable` |
| `last_synced_at` | timestamptz | Nullable |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

Constraints:

- Unique `user_id + github_repository_id`.
- A user may have at most one selected active repository in the MVP.
- Repository ownership is always scoped through `user_id`.

### `daily_tasks`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `goal_id` | uuid | References `goals.id` |
| `repository_id` | uuid | References `repositories.id`; nullable until selected |
| `scheduled_date` | date | User-local intended date |
| `title` | text | Generated task title |
| `summary` | text | Short explanation |
| `instructions` | jsonb | Structured steps |
| `acceptance_checklist` | jsonb | String array |
| `skill_tags` | jsonb | String array |
| `estimated_minutes` | integer | Generated estimate |
| `suggested_path` | text | Must pass safe-path validation |
| `generated_markdown` | text | Original validated draft |
| `user_markdown` | text | User-edited final content |
| `suggested_commit_message` | text | Original suggestion |
| `suggested_branch` | text | Verified default branch suggested for the mission; nullable for legacy rows |
| `difficulty` | text | `beginner`, `intermediate`, or `advanced`; nullable for legacy rows |
| `learning_outcome` | text | Validated mission learning outcome; nullable for legacy rows |
| `user_commit_message` | text | User-edited final message |
| `status` | text | `generating`, `generated`, `in_progress`, `ready`, `committing`, `committed`, `failed`, `archived` |
| `ai_provider` | text | Provider identifier |
| `ai_model` | text | Model identifier where available |
| `prompt_version` | text | Versioned prompt identifier; nullable for legacy rows |
| `generation_error_code` | text | Nullable allowlisted internal code; never a raw provider error |
| `generation_attempts` | integer | Monotonic claim version, default 1; never reset or reused |
| `provider_generation_attempts` | integer | Failed claim versions that reached the provider; `0` to `3` |
| `approved_at` | timestamptz | Nullable |
| `completed_at` | timestamptz | Nullable |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

Constraints:

- Unique `user_id + scheduled_date`; one task row per user-local day.
- A user has at most one active mission across `generating`, `generated`,
  `in_progress`, `ready`, and `committing`; completed, failed, and archived rows
  remain as history and do not block a later mission.
- A failed generation is retried within the same row, up to three failed claims
  that reached the provider.
- Application-input, stored-context, prompt-construction, and provider-
  configuration failures do not consume the provider-backed allowance. Input
  and configuration checks run before the atomic claim; a stored-context or
  prompt failure releases the claimed row without incrementing
  `provider_generation_attempts`. Deleting all failed-task usage rows during
  local development resets the provider-backed allowance and changes the fixed
  error category to retryable `unknown_provider_error`, while preserving the
  monotonic claim version and mission history.
- A valid generated or completed mission is never regenerated or overwritten.
- Status transitions validated in server code.
- Final content must not be empty before approval.
- Unit 10 mission fields are required when `prompt_version` is non-null and the
  task has reached `generated` or a later workflow status.

### `commits`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `task_id` | uuid | References `daily_tasks.id` |
| `repository_id` | uuid | References `repositories.id` |
| `github_commit_sha` | text | GitHub commit SHA |
| `github_commit_url` | text | GitHub URL |
| `branch` | text | Actual target branch |
| `file_path` | text | Validated target path |
| `commit_message` | text | Submitted commit message |
| `content_snapshot` | text | Exact content submitted |
| `status` | text | `created`, `reconciled`, `failed` |
| `created_at` | timestamptz | Default now |

Constraints:

- Unique `task_id` for the MVP.
- Unique `github_commit_sha`.
- Store no GitHub installation token.

### `schedules` — Phase 2

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `goal_id` | uuid | References `goals.id` |
| `timezone` | text | IANA timezone |
| `local_time` | time | Preferred local time |
| `days_of_week` | smallint[] | ISO weekday values 1–7 |
| `next_run_at` | timestamptz | Computed UTC time |
| `is_active` | boolean | Default true |
| `last_run_at` | timestamptz | Nullable |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

### `email_preferences`

| Column | Type | Notes |
|---|---|---|
| `user_id` | uuid | Primary key; references `profiles.id` |
| `welcome_enabled` | boolean | Default true |
| `daily_task_enabled` | boolean | Default true |
| `weekly_summary_enabled` | boolean | Default true |
| `connection_alerts_enabled` | boolean | Default true for security/operational alerts |
| `marketing_enabled` | boolean | Default false |
| `updated_at` | timestamptz | Default now |

### `ai_usage_records`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | References `profiles.id` |
| `task_id` | uuid | Nullable |
| `provider` | text | Provider |
| `model` | text | Model |
| `operation` | text | `task_generation`, `review`, `weekly_summary` |
| `input_units` | integer | Nullable provider measurement |
| `output_units` | integer | Nullable provider measurement |
| `estimated_cost_minor` | integer | Nullable internal estimate |
| `success` | boolean | Result |
| `provider_call_id` | uuid | Nullable for legacy rows; unique idempotency key for one provider HTTP call |
| `created_at` | timestamptz | Default now |

### `audit_logs`

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `user_id` | uuid | Nullable for verified system webhooks |
| `action` | text | Stable event name |
| `resource_type` | text | `installation`, `repository`, `task`, `commit`, etc. |
| `resource_id` | uuid | Nullable |
| `metadata` | jsonb | Sanitized details |
| `github_delivery_id` | text | Nullable; unique for webhook audit events |
| `created_at` | timestamptz | Default now |

Never store:

- Access tokens.
- Private keys.
- Full unfiltered webhook headers.
- Secret API values.
- Raw provider errors containing sensitive request information.

### Mission Generation Functions

Unit 10 adds service-role-only, fixed-search-path functions that atomically claim,
finalize, and fail daily mission generation. They independently verify the
authenticated owner, active goal, selected active repository, and active verified
installation. The claim function enforces the unique user-local day and the
three-provider-backed-failure limit. Finalize/fail operations require the current claim version,
write idempotent AI usage records, and write sanitized lifecycle audit events in
the same transaction. `public`, `anon`, and `authenticated` receive no execution
privilege on these functions.
The claim returns the user's existing active mission, including one from an
earlier local date, instead of creating another active mission.

Unit 10 removes the generic authenticated insert/update policies from
`daily_tasks`. Authenticated users retain owner-scoped reads, while mission
lifecycle writes occur only through the authenticated server action and the
service-role-only functions above. A later reviewed task-workspace unit must add
narrow mutation functions for editable fields rather than restoring broad row
updates.

### `github_webhook_deliveries`

Server-only delivery ledger for GitHub webhook idempotency and recovery.

| Column | Type | Notes |
|---|---|---|
| `id` | uuid | Primary key |
| `delivery_id` | text | Unique `X-GitHub-Delivery` GUID |
| `event_name` | text | Validated event name |
| `action` | text | Nullable validated action |
| `payload_sha256` | text | SHA-256 of the verified raw payload |
| `installation_id` | bigint | Nullable GitHub installation identifier |
| `status` | text | `processing`, `completed`, `ignored`, or `failed` |
| `attempt_count` | integer | Claim version for safe recovery |
| `claimed_at` | timestamptz | Latest claim time |
| `processed_at` | timestamptz | Nullable completion time |
| `last_error_code` | text | Nullable sanitized fixed code |
| `created_at` | timestamptz | Default now |
| `updated_at` | timestamptz | Default now |

RLS is enabled with no browser/user policy. `anon` and `authenticated` have no
table or function access. Service-role-only database functions atomically claim a
delivery, apply a verified lifecycle transition with its audit row, and reject a
stale claim version. The table never stores a raw payload, signature, secret,
installation token, repository name, account login, or user ID.

## Row Level Security Model

### User-Owned Reads

A signed-in user may select rows only when:

```sql
user_id = auth.uid()
```

For `profiles`:

```sql
id = auth.uid()
```

### User-Owned Writes

A signed-in user may insert or update rows only when the target `user_id` equals `auth.uid()`.

### Sensitive Server-Only Operations

The following happen only through trusted server boundaries:

- Upserting verified GitHub installation metadata.
- Synchronizing repository access from GitHub.
- Creating commit records after a GitHub response.
- Writing audit events for webhooks.
- Recording system-generated scheduled tasks.
- Processing uninstallation or suspension webhooks.

Service-role access is used only in server-only code after independent authentication or webhook verification.

## Delete Behaviour

- Deleting an Auth user should cascade or invoke a controlled cleanup of user-owned application data.
- Uninstalling the GitHub App does not delete task history; it marks installation and repository access inactive.
- Removing a repository marks access unavailable but preserves historical commit references.
- Deleting a goal archives associated task history rather than silently deleting completed work.

## Indexes

Add indexes for:

- `goals(user_id, status)`
- `github_installations(user_id, status)`
- `repositories(user_id, is_selected, access_status)`
- `daily_tasks(user_id, created_at desc)`
- `daily_tasks(user_id, status)`
- `daily_tasks(user_id, scheduled_date)`
- `commits(user_id, created_at desc)`
- `schedules(is_active, next_run_at)`
- `audit_logs(user_id, created_at desc)`
